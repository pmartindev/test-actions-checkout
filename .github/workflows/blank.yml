name: reproduction

on: push

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: ls -la
      - run: pwd
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo $GITHUB_TOKEN
     
